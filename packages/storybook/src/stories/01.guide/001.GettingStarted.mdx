import { Meta, Subtitle } from '@storybook/blocks'

<Meta title='Руководство/Начало работы'/>

# Начало работы
<Subtitle>
  <b>Design System</b> - это UI-платформа для реализации компонентов с различными дизайнами,
  синхронизирующихся с токенами.
</Subtitle>

## Обзор
Принцип работы системы - это разделение компонента на две части.

<b>Тело</b>, оно же конструктор - это базовые возможности компонента.
В нем разрабатывается вся механика работы компонента: свойства, базовые стили, вся функциональность.
Тело может быть только одно.

<b>Токины</b> - это все, что связано с внешним видом. Это все данные, которые мы получаем от токенов,
преобразованные в стили. Это похоже на @layer в CSS, только на уровне всего компонента.

Это нужно, чтобы одновременно можно было поддерживать множество дизайнов компонента:

* при исправлении бага в механике компонента, исправляются все другие однотипные компоненты;
* ускоряет разработку новых дизайнов компонента, так как не приходится разрабатывать компонент с нуля;
* у каждого компонента будут как свойства от конструктора, так и свои собственные токены.

## Требования к системе
Ядро UI - это Vite, <b>Vue3</b> и TypeScript:

* Vite - это инструмент сборки, цель которого - обеспечить более быструю и
экономичную разработку современных веб-проектов.
Это аналог webpack, который работает в разы быстрее.
**Vite не обязательно для работы**. Можно использовать UI на проектах, собранных на webpack.

* **Vue3** - фреймворк. **Обязательное требование**.
Vue2 имеет совсем другую архитектуру разработки, поэтому невозможно его поддерживать параллельно.

* TypeScript - это настройка над JavaScript, его главное преимущество - это типизация всех значений.
Благодаря этому редакторы понимают, что это за компонент и что он принимает.
Также TypeScript помогает разработчикам делать меньше ошибок, но кому это интересно.
**TypeScript тоже не обязателен, его можно не использовать**.
UI прекрасно работает также на проектах без него. Но не рекомендую игнорировать TS, вы влюбитесь.

## Установка
UI доступен в нескольких вариантах как в рамках сайта coral.club, так и за его пределами:

* использование как **HTML-элемент** на сайте;
* использование как отдельный проект внутри экосистемы сайта;
* подключение к проектам Vita/Webpack + **Vue3** + JavaScript/TypeScript как отдельный пакет;
* подключение к проектам **Nuxt3** как модуль.

