import { Meta, Subtitle } from '@storybook/blocks'

<Meta title='Руководство/Быстрое использование на сайте'/>

# Быстрое использование на сайте
<Subtitle>
  Рекомендуется, если нужно добавить в существующую страницу какой-то компонент.
</Subtitle>

## Обзор
<p>
  На сайте развернут глобальный проект.
  Благодаря этому, мы можем получить доступ к любому компоненту UI через HTML-код.
</p>
<p>
  <b>Тело</b>, оно же конструктор - это базовые возможности компонента.
  В нем разрабатывается вся механика работы компонента: свойства, базовые стили, вся функциональность.
  Тело может быть только одно.
</p>
<p>
  <b>Скин</b> - это все, что связано с внешним видом. Это все данные, которые мы получаем от токенов,
  преобразованные в стили. Это похоже на @layer в CSS, только на уровне всего компонента.
</p>
<p>
  Это нужно, чтобы одновременно можно было поддерживать множество дизайнов компонента.
  То есть, при исправлении бага в механике компонента, исправляются все другие однотипные компоненты.
  Также это ускоряет разработку новых дизайнов компонента, так как не приходится разрабатывать компонент с нуля.
  При этом новый компонент не будет содержать мусорный код от старого,
  если он был разработан на основе старого компонента.
  И при всем этом, у каждого компонента будут как свойства от конструктора, так и свои собственные.
</p>

## Установка
<p>UI доступен в нескольких вариантах как в рамках сайта coral.club, так и за его пределами.</p>
