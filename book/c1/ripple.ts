import {
  type StorybookArgs,
  type StorybookArgsValue
} from '../../types/storybook'

import {
  rippleArgs,
  rippleDescription,
  rippleValues
} from '../components/ripple'

export const c1RippleDescription: string = rippleDescription

export const c1RippleArgs: StorybookArgs = rippleArgs

export const c1RippleValues: StorybookArgsValue = rippleValues
