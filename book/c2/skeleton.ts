import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  skeletonArgs,
  skeletonText,
  skeletonValues
} from '../components/skeleton.ts'

export const c2SkeletonArgs: StorybookArgs = skeletonArgs

export const c2SkeletonValues: StorybookArgsValue = skeletonValues

export const c2SkeletonText = skeletonText
