import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  windowArgs,
  windowText,
  windowValues
} from '../components/window.ts'

export const c2WindowArgs: StorybookArgs = windowArgs

export const c2WindowValues: StorybookArgsValue = windowValues

export const c2WindowText = windowText
