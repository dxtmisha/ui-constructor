import {
  type StorybookArgs,
  type StorybookArgsValue
} from '../../types/storybook'

import {
  rippleArgs,
  rippleDescription,
  rippleValues
} from '../components/ripple'

export const c2RippleDescription: string = rippleDescription

export const c2RippleArgs: StorybookArgs = rippleArgs

export const c2RippleValues: StorybookArgsValue = rippleValues
