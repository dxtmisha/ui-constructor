import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  skeletonArgs,
  skeletonText,
  skeletonValues
} from '../components/skeleton.ts'

export const m2SkeletonArgs: StorybookArgs = skeletonArgs

export const m2SkeletonValues: StorybookArgsValue = skeletonValues

export const m2SkeletonText = skeletonText
