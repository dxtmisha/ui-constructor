import {
  type StorybookArgs,
  type StorybookArgsValue
} from '../../types/storybook'

import {
  rippleArgs,
  rippleDescription,
  rippleValues
} from '../components/ripple'

export const m2RippleDescription: string = rippleDescription

export const m2RippleArgs: StorybookArgs = rippleArgs

export const m2RippleValues: StorybookArgsValue = rippleValues
