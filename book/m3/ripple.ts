import {
  type StorybookArgs,
  type StorybookArgsValue
} from '../../types/storybook'

import {
  rippleArgs,
  rippleDescription,
  rippleValues
} from '../components/ripple'

export const m3RippleDescription: string = rippleDescription

export const m3RippleArgs: StorybookArgs = rippleArgs

export const m3RippleValues: StorybookArgsValue = rippleValues
