import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  maskArgs,
  maskDescription,
  maskValues
} from '../components/mask.ts'

export const M3MaskDescription: string = maskDescription

export const M3MaskArgs: StorybookArgs = maskArgs

export const M3MaskValues: StorybookArgsValue = maskValues
