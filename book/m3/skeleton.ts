import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  skeletonArgs,
  skeletonText,
  skeletonValues
} from '../components/skeleton.ts'

export const m3SkeletonArgs: StorybookArgs = skeletonArgs

export const m3SkeletonValues: StorybookArgsValue = skeletonValues

export const m3SkeletonText = skeletonText
