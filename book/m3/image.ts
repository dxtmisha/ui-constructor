import {
  type StorybookArgs,
  type StorybookArgsValue
} from '../../types/storybook'

import {
  imageArgs,
  imageText,
  imageValues
} from '../components/image'

export const m3ImageArgs: StorybookArgs = imageArgs

export const m3ImageValues: StorybookArgsValue = imageValues

export const m3ImageText = imageText
