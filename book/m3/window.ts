import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  windowArgs,
  windowDescription,
  windowValues
} from '../components/window.ts'

export const m3WindowDescription: string = windowDescription

export const m3WindowArgs: StorybookArgs = windowArgs

export const m3WindowValues: StorybookArgsValue = windowValues
