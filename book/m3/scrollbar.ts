import {
  StorybookArgs,
  StorybookArgsValue
} from '../../types/storybook.ts'

import {
  scrollbarArgs,
  scrollbarDescription,
  scrollbarValues
} from '../components/scrollbar.ts'

export const m3ScrollbarDescription: string = scrollbarDescription

export const m3ScrollbarArgs: StorybookArgs = scrollbarArgs

export const m3ScrollbarValues: StorybookArgsValue = scrollbarValues
